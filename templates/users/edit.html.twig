{% extends "./base2.html.twig" %}

{% block body %}
<div class="w-4/5 m-auto text-center">
    <div class="py-6 border-b border-gray-200">
        <h1 class="text-6xl pt-6 pb-5">
        Edit User Profile
    </h1>

    {{ form_start(form) }}

        {{ form_widget(form) }}
        
        <button    
            type="submit"
            class="uppercase mt-15 bg-blue-500 text-gray-100 text-lg  w-1/5 mt-10 font-extrabold py-4 px-8 rounded-3xl">
            Submit Post
        </button>
       <button class="uppercase mr-10 mt-15 bg-red-500 text-gray-100 text-lg w-1/5 mt-10 font-extrabold py-4 px-8 rounded-3xl">
           <a
                   href="/users">
               Cancell
           </a>
           </button>

    {{ form_end(form) }}
</div>
</div>

{% endblock %}







































































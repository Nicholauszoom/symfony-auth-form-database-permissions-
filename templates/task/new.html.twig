{% extends 'base2.html.twig' %}

{% block title %}New Task{% endblock %}

{% block body %}
{#<div class="w-4/5 m-auto text-center">#}
{#    <div class="py-6 border-b border-gray-200">#}
{#        <h1 class="text-6xl pt-6 pb-5">#}
{#            Create Tasks#}
{#        </h1>#}

{#    {{ include('task/_form.html.twig') }}#}
{#<div class="uppercase mt-15 bg-blue-500 text-gray-100 text-lg w-1/5 mt-10 font-extrabold py-4 px-8 rounded-3xl">#}
{#    <a href="{{ path('app_task_index') }}">back to list</a>#}
{#</div>#}
{#    </div>#}
{#</div>#}



    <div class="w-4/5 m-auto text-center">
        <div class="py-6 border-b border-gray-200">
            <h1 class="text-6xl pt-6 pb-5">
                Create Task Details
            </h1>
            <div class="m-auto w-4/5 my-8">
                <a
                        href="{{ path('app_task_index') }}"
                        class="uppercase border border-gray-500 text-lg py-4 px-6 rounded transition transition-all bg-gray-800 text-white hover:bg-white hover:text-gray-800">
                    back to list
                </a>
            </div>

            {{ form_start(form) }}

            {{ form_widget(form)}}

            <button
                    type="submit"
                    class="uppercase mt-15 bg-blue-500 text-gray-100 text-lg w-1/5 mt-10 font-extrabold py-4 px-8 rounded-3xl">
                create
            </button>


            {{ form_start(form) }}
        </div>
    </div>
{% endblock %}
